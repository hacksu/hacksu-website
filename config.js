var config = module.exports;

// Server configuration
config.port = 8080; // Port that the server will run on


// Sendgrid configuration
config.sendgrid = {};
config.sendgrid.apikey = ''; // Token used to access sendgrid api.
